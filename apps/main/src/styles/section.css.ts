import { cva } from "../../styled-system/css"
import * as root from "@jjh/styles/section"

export const primary=cva(root.basic)({visual:"outline",size:"lg"})
//export const section2=css(root.section2)
``