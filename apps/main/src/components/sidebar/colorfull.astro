<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        /* Reset some default styles */
        body, margin, padding {
            margin: 0;
            padding: 0;
        }

        /* Global styles */
        body {
            font-family: 'Comic Sans MS', cursive, sans-serif;
            background: linear-gradient(135deg, #FFA07A, #87CEEB); /* Gradient background */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        /* Sidebar styles */
        .sidebar {
            width: 80px;
            background: linear-gradient(to bottom, #FF69B4, #FFD700); /* Gradient sidebar background */
            color: white;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-between;
            transition: width 0.5s;
            overflow-x: hidden;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }

        .sidebar a {
            padding: 16px;
            text-decoration: none;
            font-size: 18px;
            color: white;
            display: flex;
            flex-direction: column;
            align-items: center;
            transition: 0.3s;
        }

        .sidebar a:hover {
            background: linear-gradient(to bottom, #FF6347, #FF4500); /* Gradient hover background */
        }

        .logo {
            font-size: 24px;
            padding: 20px 0;
            color: #fff;
        }

        /* Content styles */
        .content {
            flex-grow: 1;
            padding: 16px;
            text-align: center;
        }

        h2, p {
            color: #fff;
        }
    </style>
    <script defer>
        // Toggle sidebar function
        function toggleSidebar() {
            const sidebar = document.querySelector('.sidebar');
            sidebar.style.width = sidebar.style.width === '80px' ? '250px' : '80px';
        }

        // Initialize the application
        document.addEventListener('DOMContentLoaded', () => {
            // Add click event listeners to the sidebar links
            const sidebarLinks = document.querySelectorAll('.sidebar a');
            sidebarLinks.forEach(link => link.addEventListener('click', toggleSidebar));
        });
    </script>
    <title>Colorful Sidebar</title>
</head>
<body>

    <div class="sidebar">
        <div class="logo">
            <i class="fas fa-rainbow"></i> <!-- Rainbow icon -->
        </div>
        <a href="#" data-target="home"><i class="fas fa-sun"></i> Home</a>
        <a href="#" data-target="profile"><i class="fas fa-star"></i> Profile</a>
        <a href="#" data-target="messages"><i class="fas fa-heart"></i> Messages</a>
        <a href="#" data-target="settings"><i class="fas fa-cog"></i> Settings</a>
    </div>

    <div class="content">
        <h2>Colorful Main Content</h2>
        <p>This is where your colorful main content goes.</p>
    </div>

</body>
</html>
