<!DOCTYPE html>
<html lang="en">
<head>
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
:root {
  --switch-width: 60px;
  --switch-height: 34px;
  --slider-background: #f1c40f; /* Yellow */
  --slider-color: #3498db; /* Blue */
  --slider-transition: 0.4s;
  --slider-border-radius: 17px; /* Half of height for rounded effect */
}

.switch {
  position: relative;
  display: inline-block;
  width: var(--switch-width);
  height: var(--switch-height);
}

.switch input { 
  opacity: 0;
  width: 0;
  height: 0;
}

.slider {
  position: absolute;
  cursor: pointer;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: var(--slider-background);
  transition: background-color var(--slider-transition);
}

.slider:before {
  position: absolute;
  content: "";
  height: 26px;
  width: 26px;
  left: 4px;
  bottom: 4px;
  background-color: white;
  transition: transform var(--slider-transition);
}

input:checked + .slider {
  background-color: var(--slider-color);
}

input:focus + .slider {
  box-shadow: 0 0 1px var(--slider-color);
}

input:checked + .slider:before {
  transform: translateX(26px);
}

/* Rounded sliders */
.slider.round {
  border-radius: var(--slider-border-radius);
}

.slider.round:before {
  border-radius: 50%;
}
</style>
</head>
<body>

<h2>Youthful Toggle Switch</h2>

<label class="switch">
  <input type="checkbox">
  <span class="slider"></span>
</label>

<label class="switch">
  <input type="checkbox" checked>
  <span class="slider"></span>
</label><br><br>

<label class="switch">
  <input type="checkbox">
  <span class="slider round"></span>
</label>

<label class="switch">
  <input type="checkbox" checked>
  <span class="slider round"></span>
</label>

</body>
</html>
